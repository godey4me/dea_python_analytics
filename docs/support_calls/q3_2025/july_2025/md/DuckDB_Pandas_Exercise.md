## DuckDB and Pandas Exercise

### Exercise

1. Use the `json_data` variable in `sample_data.py` to create a pandas DataFrame.

2. Load the DataFrame as a table in a new DuckDB database called `sample.db` and call the table as `raw_data`.

3. Create a SQL query that only returns the unique records and save the result as a new table called `clean_data`.